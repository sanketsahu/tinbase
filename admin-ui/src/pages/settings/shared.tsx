// The settings grammar lives in the shared layout so other sections
// (Realtime → Settings, Edge Functions → Settings) render the same way.
export { Fact, SettingsRow, SettingsSection, SettingsShell } from '../../components/layout/settings'
